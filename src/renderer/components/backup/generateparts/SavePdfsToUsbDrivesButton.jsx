import React from 'react'

import Button from '../../Button'

const SavePdfsToUsbDrivesButton = ({
	generatorState,
	setGeneratorState,
	STATES,
}) => {
	return (
		<>
			<Button>Save PDFs to USB Drives</Button>
		</>
	)
}

export default SavePdfsToUsbDrivesButton
